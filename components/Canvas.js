'use client';   

import { useRef, useEffect } from 'react';

const Canvas = ({ currentTool, addImage }) => {
    return (
        <div>
            this is canvas
        </div>
    );
}

export default Canvas;